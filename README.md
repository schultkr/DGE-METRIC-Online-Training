# DGE-METRIC Online Training (Training Day 3–5)

Online continuation of the on-site DGE-METRIC training, numbered Training Day 3–5 / Online Day 1–3. Dates: Wednesday 22 – Friday 24 July 2026.

## Contents

- [`Agenda/`](Agenda/) — session-by-session agenda (`detailed-agenda.md` is the source of truth; the `.docx` is the participant-facing export)
- [`slides/`](slides/) — one facilitator `.pptx` per online day, plus the Python build scripts that generate them
- [`Code/`](Code/) — hands-on code used in the training
  - [`Code/reserve-requirements/`](Code/reserve-requirements/) — break-even toy model for the reserve-requirements sessions
  - [`Code/dge-metric-model/`](Code/dge-metric-model/) — DGE-METRIC model code (`ModFiles/`, `Functions/`). **Partial transfer in progress** — see its `TODO.md` for what's still missing and an open scenario-design question that needs resolving before the finance-scenario hands-on session can run against this code.
